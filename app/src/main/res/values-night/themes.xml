<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.News" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryDark">@color/errie_black</item>
        <item name="android:colorBackground">@color/errie_black</item>
        <item name="colorSurface">@color/errie_black</item>
    </style>

    <style name="News.Logo" parent="">
        <item name="tint">@color/white</item>
    </style>
</resources>